import React from 'react';
import BookInfo from '../../components/BookInfo/BookInfo';

const Saved = () => {
    return (
        <div>
            <BookInfo />
        </div>
    );
};

export default Saved;